[package]
name = "mdviewy-utils"
version = "0.0.1"
edition = "2021"
description = "mdviewy utils"
license = "MIT"

[dependencies]
fs_extra = "1.2.0"
serde = { version = "1.0", features = ["derive"] }
